import solution from '.';

const day = __filename.match(/\/([^/]+)\/spec/)[1];
const title = '';

describe(`Day ${day} - ${title}`, () => {
  test('test case', () => {
    expect(true).toEqual(true);
  });
});
